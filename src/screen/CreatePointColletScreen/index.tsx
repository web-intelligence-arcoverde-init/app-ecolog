import React from 'react';

import {Container, Typography, Button} from '../../components';

const CreatePointCollectScreen = () => {
  return (
    <Container>
      <Typography>primeiro passo para um ambiente mais verde</Typography>

      <Button>Crie um ponto para retirada</Button>
    </Container>
  );
};

export default CreatePointCollectScreen;
