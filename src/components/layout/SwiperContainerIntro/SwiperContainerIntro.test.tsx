import 'react-native';
import React from 'react';
import {render} from '@testing-library/react-native';
import {SwiperContainerIntro} from './SwiperContainerIntro';

describe('Name of the LayoutSwiperContainer', () => {
  it('renders default correctly LayoutSwiperContainer', () => {
    render(<SwiperContainerIntro />);
  });
});
