import 'react-native';
import React from 'react';
import {FooterIntroScreen} from './FooterIntroScreen';
import {render} from '@testing-library/react-native';

it('renders correctly Typography', () => {
  render(<FooterIntroScreen />);
});
